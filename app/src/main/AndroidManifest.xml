<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iameben.mycontacts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyContacts">
        <activity
            android:name=".FriendsContactActivity"
            android:label="@string/friends_contacts" />
        <activity android:name=".FamilyContactActivity"
            android:label="@string/family_contacts"/>
        <activity android:name=".FavouriteContactActivity"
            android:label="@string/favourite_contacts"/>
        <activity android:name=".AllContactActivity"
            android:label="@string/all_contacts"/>
        <activity android:name=".CategoryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>